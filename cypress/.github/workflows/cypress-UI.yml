name: Cypress Automation Exercise UI Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # Allows manual trigger

jobs:
  cypress-ui:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Run Cypress UI tests
<<<<<<< HEAD
        run: npx cypress run --spec "cypress/e2e/UI-AutomationExercise"
=======
        run: npx cypress run --spec "cypress/e2e/UI-AutomationExercise"
>>>>>>> 6b6803934860e87914957cd0b36cc25029510a03
